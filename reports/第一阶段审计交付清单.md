# 第一阶段审计交付清单

生成日期：2026-08-12  
项目目录：`/gpfsdata/home/shitengyuan/shitengyuan_lustre/medical/tcga-stad`  
生成版本：`59bea3917395ca83bbbfb3771ea97ae954bdd9f1` 基础上新增审计脚本和第一阶段输出

## 一、已经交付的材料

### 1. 289例内部TCGA患者级队列表

文件：

- `results/audit_first_stage/final_289_patient_cohort.csv`
- `results/audit_first_stage/case_flow_statistics.json`

内容：

- `patient_id`
- `site`
- `MSI`
- `EBV`
- `M1_label`
- `M4_subtype`
- `M4_label`
- `POLE`
- `slide_id`
- `include_status`
- `include_exclude_reason`
- M1-M5 OOF概率
- 重建的 `fold`

当前限制：

- `clinical.csv` 不在当前仓库，`slide_id` 和 `POLE` 无法从现有OOF结果恢复，已保留为空值并写明原因。
- 原始队列到289例的完整流转只能部分恢复；当前可恢复统计为：OOF 289例，M1阳性73例、阴性216例；M4四亚型为 EBV 26、MSI 47、GS 45、CIN 171。

### 2. CV分折重建和泄漏核查

文件：

- `results/audit_first_stage/cv_folds_reconstructed.csv`
- `results/audit_first_stage/cv_fold_reconstruction_summary.json`

结论：

- 按原源码策略重建：`seed=42`，5-fold x 3 repeats，`StratifiedGroupKFold`，group 为 TCGA tissue source site。
- 当前289例里同一患者不跨验证折。
- 当前289例里同一site不跨验证折。
- 当前289例中没有“小站点只进train”的患者。

当前限制：

- 原始fold文件当时没有落盘，因此本文件是按现有289例OOF患者重建，不是训练时原始保存文件。
- 若找回 `clinical.csv`，需要重新生成包含所有候选患者和小站点患者的正式fold登记表。

### 3. M1-M5 OOF正式指标重算

文件：

- `results/audit_first_stage/metrics_M1-M5_oof_recomputed.json`
- `results/audit_first_stage/metrics_M1_immune_sensitive_oof_recomputed.json`
- `results/audit_first_stage/metrics_M2_msi_oof_recomputed.json`
- `results/audit_first_stage/metrics_M3_ebv_oof_recomputed.json`
- `results/audit_first_stage/metrics_M4_subtype4_oof_recomputed.json`
- `results/audit_first_stage/metrics_M5_clinical_oof_recomputed.json`

已同步覆盖：

- `results/metrics_M1_immune_sensitive.json`
- `results/metrics_M2_msi.json`
- `results/metrics_M3_ebv.json`
- `results/metrics_M4_subtype4.json`
- `results/metrics_M5_clinical.json`

旧版备份：

- `results/audit_first_stage/legacy_metrics_backup_20260812_233315/`

指标内容：

- ROC-AUC
- PR-AUC / average precision
- 患者级bootstrap 95% CI
- 固定阈值0.5下的敏感度、特异度、PPV、NPV、F1、混淆矩阵
- Brier score
- calibration bins
- calibration intercept/slope

M1重算结果示例：

- n=289
- ROC-AUC=0.8986
- PR-AUC=0.7825
- 0.5阈值：敏感度=0.6986，特异度=0.9167，PPV=0.7391，NPV=0.9000
- 混淆矩阵：TN=198，FP=18，FN=22，TP=51

### 4. M4类别映射核查

文件：

- `results/audit_first_stage/m4_class_mapping_audit.csv`
- `results/audit_first_stage/m4_class_mapping_audit.json`

确认顺序：

```text
prob_c0 = EBV
prob_c1 = MSI
prob_c2 = GS
prob_c3 = CIN
```

结论：

- 训练源码和标签映射均为 `[EBV, MSI, GS, CIN]`。
- 所有289例 `label` 与 `subtype` 映射一致。
- `TCGA-VQ-AA69` 的 `prob_c1=0.9038` 实际指向 MSI，不是 EBV；已有Agent病例报告对此存在解释不一致。

### 5. 标签来源和映射说明

文件：

- `results/audit_first_stage/label_sources_and_mapping.json`
- `build_tcga_label_table.py`

已整理：

- TCGA内部任务映射规则。
- CPTAC标签字段、来源文件和QC策略。
- unknown / conflict 值处理原则。

当前限制：

- 内部TCGA原始 `clinical.csv` 缺失，无法恢复原始MSI、EBV、TCGA subtype字段和POLE字段。
- 已新增 `build_tcga_label_table.py`，找回 `clinical.csv` 后可重建完整原始标签表和病例流转。

### 6. Agent泄漏核查和无泄漏方案

文件：

- `results/audit_first_stage/agent_leakage_audit_and_no_leakage_plan.json`

结论：

- 当前 `results/agent_panel_judgments.json` 只能作为开发演示。
- 原因：panel输入含真实 `subtype`，且当前panel视觉模块已关闭。
- 在视觉证据包补齐前，不应输出未经图像支持的TIL、CIN或MSI形态学结论。

后续无泄漏版本要求：

- 移除真实MSI、EBV、TCGA subtype和标签。
- 只允许输入模型概率、允许使用的非分子临床变量和真实视觉材料。
- `final_pred` 继续固定为M1阈值输出，Agent只报告置信度、冲突和建议验证项目。

### 7. CPTAC外部验证第一阶段结果

文件：

- `results/audit_first_stage/cptac_193_feature_inference/external_feature_slide_predictions.csv`
- `results/audit_first_stage/cptac_193_feature_inference/external_feature_patient_predictions.csv`
- `results/audit_first_stage/cptac_slide_cohort_and_exclusions.csv`
- `results/audit_first_stage/cptac_patient_cohort_labels_predictions_fixed_threshold.csv`
- `results/audit_first_stage/cptac_fixed_threshold_metrics_bootstrap.json`

结论：

- CPTAC manifest：50个患者，204张SVS。
- 目前有193个20x256特征文件，已全部完成推理。
- 11张切片缺少特征，排除原因记录为 `feature_missing`。
- 患者级预测50例。
- 有标签可评价47例。
- 固定阈值0.5，不在CPTAC上重新选择阈值。

M1外部验证结果：

- n=47
- ROC-AUC=0.8375
- PR-AUC=0.7212
- 敏感度=0.8667
- 特异度=0.8750
- PPV=0.7647
- NPV=0.9333
- 混淆矩阵：TN=28，FP=4，FN=2，TP=13

### 8. 最小复现材料

文件：

- `results/audit_first_stage/environment_minimal.yml`
- `results/audit_first_stage/reproducibility_manifest.json`
- `results/audit_first_stage/README.md`
- `audit_first_stage.py`
- `build_tcga_label_table.py`

记录内容：

- Python版本
- PyTorch/CUDA版本
- 主要依赖
- 随机种子
- 当前commit
- 模型权重hash
- UNI2-h权重hash
- 重跑命令

## 二、仍缺少的材料

### 1. 内部TCGA原始数据和完整标签表

缺少：

- `clinical.csv`
- `tcga_stad_uni2h/TCGA-STAD/features`
- 原始MSI字段
- 原始EBV字段
- 原始TCGA subtype字段
- POLE突变字段
- 患者到全部切片的真实 `slide_id` 列表

影响：

- 无法完整恢复从原始队列到289例的所有纳入/排除步骤。
- 无法补齐 `slide_id` 和 `POLE`。
- 无法证明当时训练fold与当前重建fold完全一致。

已准备的补救：

- `build_tcga_label_table.py` 可在找回 `clinical.csv` 后重建完整标签表和病例流转统计。

### 2. 原始CV fold文件

缺少：

- 训练时实际保存的每repeat、每fold train/val患者列表。

影响：

- 当前只能按源码策略重建fold。
- OOF CSV中没有fold列，无法逐患者确认训练时fold归属。

### 3. 每fold模型权重和训练日志

缺少：

- M1-M6每fold正式权重。
- best epoch。
- early stopping日志。
- 每epoch train/val loss、AUC。
- 超参搜索记录。
- 多随机种子重复运行记录。

当前说明：

- `models/M1-M4.pt` 从源码看是每个任务训练循环最后一个fold保存的模型，不是每fold全套权重，也不能证明是全数据重训权重。

### 4. 图像证据包

缺少：

- 三个展示病例 AA69、AA6K、BR-7703 的WSI缩略图。
- attention heatmap。
- cluster overlay。
- 代表patch。
- Agent真实视觉输入输出。

影响：

- 不能把现有Agent报告作为正式形态学证据。

### 5. Agent正式无泄漏全量结果

缺少：

- 移除真实标签后的全量Agent结构化输出。
- 重复运行稳定性。
- prompt版本登记。
- JSON Schema和工具定义版本登记。

### 6. 聚类命名和病理验证

缺少：

- 每个cluster的代表patch。
- 聚类中心。
- cluster分布。
- TIL/肿瘤/间质/坏死命名依据。
- 病理医师验证记录。

### 7. 更完整统计分析

缺少：

- M1-M4全部错误病例清单。
- 按中心、Lauren分型、部位、分期、切片质量等亚组结果。
- 决策曲线。
- M1与M5配对统计比较。
- 图像+临床融合模型真实结果。

## 三、建议下一阶段优先级

1. 找回或重建 `clinical.csv`，运行 `build_tcga_label_table.py`，补齐 TCGA 原始标签、POLE、slide_id 和完整病例流转。
2. 重新训练正式实验版本，并保存每fold权重、best epoch、日志、配置和fold文件。
3. 重跑无泄漏Agent：去掉真实subtype/MSI/EBV标签，暂不输出没有图像支持的形态学结论。
4. 补三例和15-20例代表病例图像证据包。
5. 完成错误病例、亚组、DCA、M1 vs M5配对比较和图像+临床融合分析。

## 四、重跑命令

```bash
cd /gpfsdata/home/shitengyuan/shitengyuan_lustre/medical/tcga-stad

/gpfsdata/home/shitengyuan/miniconda3/envs/gastric_msi_pathai/bin/python audit_first_stage.py \
  --device cpu \
  --n_boot 2000
```

找回 `clinical.csv` 后：

```bash
cd /gpfsdata/home/shitengyuan/shitengyuan_lustre/medical/tcga-stad

/gpfsdata/home/shitengyuan/miniconda3/envs/gastric_msi_pathai/bin/python build_tcga_label_table.py \
  --clinical_csv clinical.csv \
  --feature_dir tcga_stad_uni2h/TCGA-STAD/features
```
