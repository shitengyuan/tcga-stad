# 材料需求逐项对照交付说明

日期：2026-08-15  
项目目录：`/gpfsdata/home/shitengyuan/shitengyuan_lustre/medical/tcga-stad`  
当前仓库 commit：`d9fd184fc1c0e586da5840feb899454262879555`  
当前分支：`main`

## 0. 总体结论

当前项目可以交付一批“清单、重建分折、患者级 OOF、重算指标、CPTAC 外部验证、部分 Agent 开发输出、复现脚本和轻量打包材料”。但内部 TCGA 原始 `clinical.csv`、TCGA h5 特征、训练时原始 fold 文件、每折正式权重、完整训练日志、WSI 视觉证据包和正式无泄漏 Agent 重跑结果不完整，因此不能把当前材料包装成完全闭环的论文级固定实验版本。

2026-08-16 进展更新：

| 新增材料 | 路径 |
|---|---|
| 不可交付材料任务清单与执行进展 | `reports/不可交付材料任务清单与执行进展.md` |
| 机器可读任务台账 | `results/cpu_supplement/missing_materials_task_list.csv`、`results/cpu_supplement/missing_materials_task_list.json` |
| 内部 OOF 高敏感度筛查阈值表 | `results/cpu_supplement/high_sensitivity_thresholds_oof.csv`、`results/cpu_supplement/high_sensitivity_thresholds_oof.json` |
| 三个展示病例现有证据包 | `results/cpu_supplement/showcase_cases_existing_evidence.csv`、`results/cpu_supplement/showcase_cases_existing_evidence.json` |
| 生成脚本 | `generate_missing_materials_followup.py` |

同时已改造未来重训脚本：

- `src/train_multitask.py`：未来重训 M1-M4 时保存 per-fold checkpoint、fold registry、best epoch、seed 和配置。
- `src/train_clinical.py`：未来重训 M5 时保存每折 sklearn pipeline、fold registry 和临床变量缺失率。
- `src/train_survival.py`：未来重训 M6 时保存患者级 OOF risk 表和 fold registry。

需要区分两个版本：

| 类型 | commit / 说明 |
|---|---|
| 当前仓库版本 | `d9fd184fc1c0e586da5840feb899454262879555` |
| 审计/交付材料内部记录的生成版本 | 多数 manifest 记录 `fa3814cb274e5c9732a2b493ef5f980e39af20a4` |
| 第一阶段审计历史版本 | `f23ca86eea293e8094b0e874f66a84c5a8689830`、`fa3814c`、`d9fd184` 相关提交 |

当前可直接交付的轻量包：

| 材料 | 路径 |
|---|---|
| GPU 刷新交付包 | `results/gpu_deliverables/package/gpu_deliverables_20260815_140458_refreshed.tar.gz` |
| GPU 交付包文件清单 | `results/gpu_deliverables/package/package_filelist.txt` |
| 第一阶段审计目录 | `results/audit_first_stage/` |
| CPU 补充材料目录 | `results/cpu_supplement/` |
| CPTAC 20x256 四卡外部推理目录 | `results/external_cptac_feature_infer_20x256_4gpu/` |
| 中文 CPTAC 外部验证报告 | `reports/CPTAC-STAD外部验证报告.md` |

## 1. 第一批材料逐项对照

### 1.1 最终患者-切片-标签清单

需求：包含 `patient_id`、`slide_id`、`center`、MSI、EBV、TCGA 四亚型、POLE、纳入/排除原因及 CV fold。

状态：部分可交付。

可交付材料：

| 文件 | 内容 |
|---|---|
| `results/audit_first_stage/final_289_patient_cohort.csv` | 289 例 TCGA OOF 患者表，含 `patient_id`、`site`、MSI、EBV、M4 subtype、M1-M5 OOF 概率、纳入状态、重建 fold |
| `results/audit_first_stage/case_flow_statistics.json` | 289 例队列流转的可恢复统计 |
| `results/audit_first_stage/cptac_slide_cohort_and_exclusions.csv` | CPTAC 患者-切片清单、特征/推理状态、排除原因 |
| `results/audit_first_stage/cptac_patient_cohort_labels_predictions_fixed_threshold.csv` | CPTAC 患者级标签和预测合并表 |

不能完整交付的部分：

- 内部 TCGA `slide_id` 当前无法完整恢复；`final_289_patient_cohort.csv` 中 `slide_id` 为空。
- POLE 当前无法恢复；`final_289_patient_cohort.csv` 中 `POLE` 为空。
- 原因：当前 checkout 中缺少 `clinical.csv` 和 `tcga_stad_uni2h/TCGA-STAD/features/`，只能从 OOF CSV 反推 289 例患者，不能恢复原始患者-全部切片映射和 POLE 字段。

### 1.2 实际交叉验证分折文件

状态：部分可交付，属于“重建分折”，不是训练时原始落盘文件。

可交付材料：

| 文件 | 内容 |
|---|---|
| `results/audit_first_stage/cv_folds_reconstructed.csv` | 5-fold x 3 repeats 重建 train/val 分折 |
| `results/audit_first_stage/cv_fold_reconstruction_summary.json` | 分折策略和泄漏检查 |

已知策略：

- `StratifiedGroupKFold`
- `seed=42`
- `n_folds=5`
- `n_repeats=3`
- group 为 TCGA tissue source site，即 `patient_id` 第二段，如 `TCGA-BR-*` 的 `BR`
- 当前 289 例中同一患者不跨验证折，同一 site 不跨验证折

不能完整交付的部分：

- 训练时实际保存的 fold 文件不存在。
- 当前 fold 是按现有 289 例 OOF 患者和源码策略重建，不能证明与训练时患者排序完全一致。

### 1.3 M1-M6 全部患者级 OOF 预测

状态：M1-M5 可交付；M6 不可完整交付。

可交付材料：

| 模型 | 文件 |
|---|---|
| M1 immune-sensitive | `results/oof_preds_M1_immune_sensitive.csv` |
| M2 MSI | `results/oof_preds_M2_msi.csv` |
| M3 EBV | `results/oof_preds_M3_ebv.csv` |
| M4 四分类 | `results/oof_preds_M4_subtype4.csv` |
| M5 临床基线 | `results/oof_preds_M5_clinical.csv` |
| M1-M5 合并表 | `results/audit_first_stage/final_289_patient_cohort.csv` |

M4 四分类概率可交付，类别顺序已核查：

```text
prob_c0 = EBV
prob_c1 = MSI
prob_c2 = GS
prob_c3 = CIN
```

相关核查文件：

- `results/audit_first_stage/m4_class_mapping_audit.csv`
- `results/audit_first_stage/m4_class_mapping_audit.json`

M6 不能完整交付：

- 仅有 `results/metrics_M6_survival.json`。
- 没有找到 M6 患者级 OOF risk 表、每 fold 风险输出、模型权重或完整评估输入表。

### 1.4 各模型 metrics JSON、ROC、bootstrap CI、阈值、混淆矩阵计算脚本

状态：M1-M5 基本可交付；M6 部分可交付；图像 ROC 图主要在 CPTAC 外部验证目录，内部 OOF ROC 原图未完整看到。

可交付材料：

| 类型 | 路径 |
|---|---|
| M1-M5 重算 metrics | `results/audit_first_stage/metrics_M1-M5_oof_recomputed.json` |
| 单模型重算 metrics | `results/audit_first_stage/metrics_M1_immune_sensitive_oof_recomputed.json` 等 |
| 当前根目录 metrics | `results/metrics_M1_immune_sensitive.json` 到 `results/metrics_M6_survival.json` |
| 错例表 | `results/cpu_supplement/errors_M*_oof.csv` |
| 校准曲线原始数据 | `results/cpu_supplement/calibration_curve_data_oof.csv` |
| 决策曲线原始数据 | `results/cpu_supplement/decision_curve_data_oof.csv` |
| 配对比较 | `results/cpu_supplement/paired_model_comparison_M1_vs_M5.json` |
| 计算脚本 | `audit_first_stage.py`、`generate_cpu_supplement.py`、`run_threshold_eval.py`、`run_plots.py`、`run_plots2.py` |

不能完整交付的部分：

- M6 只有 C-index metrics JSON，没有患者级 OOF 风险表和完整可复核中间输出。
- 内部 OOF 的正式 ROC 图片未完整整理为独立交付目录；但指标和曲线原始数据可交付。

### 1.5 标签构建、特征加载、分折、训练到评价的完整源码、配置、依赖和运行命令

状态：源码和最小依赖可交付；完整原始数据依赖不可交付。

可交付材料：

| 内容 | 路径 |
|---|---|
| 标签表重建脚本 | `build_tcga_label_table.py` |
| 特征加载 | `src/feature_loader.py` |
| M1-M4 训练 | `src/train_multitask.py` |
| M5 训练 | `src/train_clinical.py` |
| M6 训练 | `src/train_survival.py` |
| ABMIL 模型 | `src/abmil.py` |
| 审计重算 | `audit_first_stage.py` |
| CPU 补充 | `generate_cpu_supplement.py` |
| CPTAC 特征提取 | `extract_cptac_uni2h_20x256.py` |
| CPTAC 特征推理 | `eval_cptac_features.py`、`run_cptac_feature_inference_4gpu.sh` |
| CPTAC 绘图 | `plot_cptac_feature_results.py` |
| 最小环境 | `results/audit_first_stage/environment_minimal.yml` |
| 复现 manifest | `results/audit_first_stage/reproducibility_manifest.json` |
| GPU 总控脚本 | `run_gpu_deliverables.sh` |
| GPU 运行配置 | `results/gpu_deliverables/gpu_deliverables_run_config.json` |

不能完整交付的部分：

- 内部训练复现需要 `clinical.csv` 和 `tcga_stad_uni2h/TCGA-STAD/features/`，当前缺失。
- 当前没有完整 conda lockfile 或 Docker 镜像，只能交付 `environment_minimal.yml`。

### 1.6 M5 临床变量、缺失率、预处理方式，以及图像+临床融合模型是否完成

状态：变量和预处理方式可从源码交付；缺失率和实际拟合对象不可交付；图像+临床融合模型未完成。

可交付材料：

| 内容 | 路径 |
|---|---|
| M5 源码 | `src/train_clinical.py` |
| M5 OOF | `results/oof_preds_M5_clinical.csv` |
| M5 metrics | `results/audit_first_stage/metrics_M5_clinical_oof_recomputed.json` |
| M1 vs M5 比较 | `results/cpu_supplement/paired_model_comparison_M1_vs_M5.json` |

M5 使用变量：

- `age`
- Lauren 分型，从 `histological_diagnosis` 派生
- `sex`
- `ajcc_pathologic_tumor_stage` 简化 stage
- `primary_site_patient`
- `lymph_node_examined_count`

预处理：

- 数值变量 median imputation
- 标准化 `StandardScaler`
- 类别变量 one-hot
- `LogisticRegression(max_iter=1000, class_weight="balanced", C=0.5)`

不能完整交付：

- 缺失率不能计算，因为当前无 `clinical.csv`。
- M5 fitted sklearn pipeline 未保存。
- 未看到图像+临床融合模型的正式训练、OOF 或 metrics；当前 M5 是临床基线，不是融合模型。

### 1.7 Agent 代码、完整提示词、工具定义、输入输出 JSON，说明实际使用模型

状态：开发版 Agent 可交付；正式无泄漏 Agent 只有输入包/提示词/schema，未完成全量重跑。

可交付材料：

| 内容 | 路径 |
|---|---|
| Agent 主代码 | `src/agent.py` |
| Agent panel | `run_agent_panel.py` |
| Friday 客户端 | `src/friday_client.py` |
| 旧 Agent 输出 | `results/agent_judgments.json` |
| panel 输出 | `results/agent_panel_judgments.json` |
| Agent 评估 | `results/agent_eval.json` |
| 无泄漏输入表 | `results/cpu_supplement/agent_no_leakage_inputs_289.csv` |
| 无泄漏 prompt | `results/cpu_supplement/agent_no_leakage_prompt.txt` |
| 无泄漏输出 schema | `results/cpu_supplement/agent_no_leakage_output_schema.json` |
| 泄漏审计 | `results/audit_first_stage/agent_leakage_audit_and_no_leakage_plan.json` |

模型使用情况：

- `src/agent.py` 默认：视觉 `glm-4v-plus`，文本 `glm-5.2`
- `run_agent_panel.py` 实际 report model：`deepseek-v3-friday`
- `run_agent_panel.py` 中视觉模块调用 `glm-4v-plus` 的代码存在，但当前 panel 输出记录为 `visual module removed`

不能完整交付：

- 没有正式无泄漏全量 LLM 重跑输出。
- 没有稳定性重复运行结果。
- 工具定义主要是 Friday API 客户端代码，没有独立版本化 JSON tool registry。

### 1.8 Agent 是否读取真实 MSI/EBV/TCGA 亚型标签，是否能修改 M1 结果，high/low 置信度规则

状态：可交付审计结论。

可交付材料：

- `results/audit_first_stage/agent_leakage_audit_and_no_leakage_plan.json`
- `run_agent_panel.py`
- `results/cpu_supplement/agent_no_leakage_inputs_289.csv`

结论：

- 当前 `results/agent_panel_judgments.json` 是开发演示，不应作为正式无泄漏 Agent 结果。
- 原因：`run_agent_panel.py` 的临床输入包含 `subtype`，即真实 TCGA 分子亚型相关信息。
- 设计上 `final_pred = M1 pred`，Agent 不应修改 M1 结果。
- 无泄漏输入包已经移除了 `label`、MSI、EBV、TCGA subtype、M4 subtype、POLE。

置信度规则来自 `run_agent_panel.py`：

- M1 概率在 `0.4-0.6`：low
- M1 与 M2/M3 冲突：low
- M1 与 M2/M3 支持一致：high
- 其他：medium

### 1.9 三个展示病例 AA69、AA6K、BR-7703 完整证据包

状态：只能交付模型概率和 Agent 原始 JSON；不能交付完整视觉证据包。

可交付材料：

| 内容 | 路径 |
|---|---|
| Agent panel 原始输出 | `results/agent_panel_judgments.json` |
| 289 例合并概率表 | `results/audit_first_stage/final_289_patient_cohort.csv` |
| M1-M5 OOF 原始表 | `results/oof_preds_M*_*.csv` |
| M4 概率 | `results/oof_preds_M4_subtype4.csv` |

不能交付：

- WSI 缩略图
- attention heatmap
- cluster overlay
- 代表 patch
- Agent 的真实视觉输入图片

原因：

- 当前目录下未找到这三个病例对应的 thumbnail、attention、cluster overlay 文件。
- `results/agent_panel_judgments.json` 明确记录 `visual module removed (unreliable on overlay images)`。

### 1.10 聚类簇命名为 TIL/肿瘤/间质/坏死的依据及病理医师验证

状态：不可交付正式证据。

当前可交付：

- Agent 代码和旧输出中有对 TIL 等形态的文本描述，但不构成聚类簇命名依据。

不能交付：

- cluster 中心
- 代表 patch
- 每簇分布
- 命名依据表
- 病理医师验证记录

原因：

- 当前项目没有找到 cluster center、representative patch 或病理医师标注文件。
- 已有审计材料明确提示：在视觉证据包补齐前，不应输出未经图像支持的 TIL、CIN、MSI 形态学结论。

### 1.11 MSI、EBV 和四亚型标签的原始字段、来源、映射规则及未知/冲突值处理

状态：CPTAC 可交付；TCGA 内部只能部分交付。

可交付材料：

| 内容 | 路径 |
|---|---|
| 标签来源和映射 | `results/audit_first_stage/label_sources_and_mapping.json` |
| TCGA 标签重建脚本 | `build_tcga_label_table.py` |
| CPTAC QC-pass 标签 | `/gpfsdata/home/shitengyuan/shitengyuan_lustre/medical/dataset/cptac-stad-histopathology/labels/cptac_stad_2026_tcga_subtype_labels_qc_pass.csv` |
| CPTAC 原始补充表 | `/gpfsdata/home/shitengyuan/shitengyuan_lustre/medical/dataset/cptac-stad-histopathology/labels/cptac_stad_2026_supplement/1-s2.0-S2666379126001734-mmc2.xlsx` |

TCGA 映射规则可交付：

- M1：`STAD_MSI` 或 `STAD_EBV` 为 1；`STAD_GS` 或 `STAD_CIN` 为 0；POLE、NO_SUBTYPE、unknown 排除。
- M2：`STAD_MSI` 为 1；`STAD_EBV/STAD_GS/STAD_CIN` 为 0。
- M3：`STAD_EBV` 为 1；`STAD_MSI/STAD_GS/STAD_CIN` 为 0。
- M4：`STAD_EBV=0`、`STAD_MSI=1`、`STAD_GS=2`、`STAD_CIN=3`。

不能完整交付：

- TCGA 原始字段名和值域不能完整复核，因为 `clinical.csv` 缺失。

### 1.12 POLE 病例清单和排除规则

状态：排除规则可交付；病例清单不可交付。

可交付：

- `src/feature_loader.py`
- `results/audit_first_stage/label_sources_and_mapping.json`

规则：

- M1 中 POLE/NO_SUBTYPE/unknown 不进入建模。
- M2/M3/M4 中非显式 subtype 映射值被排除。

不能交付：

- POLE 病例清单。

原因：

- 当前 OOF 文件不含 POLE 字段。
- `clinical.csv` 缺失，无法恢复 POLE 病例。

### 1.13 CPTAC 外部验证材料

状态：可交付，且已较完整。

可交付材料：

| 内容 | 路径 |
|---|---|
| CPTAC 标签 | `/gpfsdata/home/shitengyuan/shitengyuan_lustre/medical/dataset/cptac-stad-histopathology/labels/cptac_stad_2026_tcga_subtype_labels_qc_pass.csv` |
| CPTAC 队列/排除表 | `results/audit_first_stage/cptac_slide_cohort_and_exclusions.csv` |
| CPTAC 患者级预测 | `results/audit_first_stage/cptac_patient_cohort_labels_predictions_fixed_threshold.csv` |
| CPTAC 固定阈值 metrics | `results/audit_first_stage/cptac_fixed_threshold_metrics_bootstrap.json` |
| CPTAC 推理原始输出 | `results/external_cptac_feature_infer_20x256_4gpu/external_feature_patient_predictions.csv` |
| CPTAC 切片级预测 | `results/external_cptac_feature_infer_20x256_4gpu/external_feature_slide_predictions.csv` |
| CPTAC 图表 | `results/external_cptac_feature_infer_20x256_4gpu/figures/` |
| CPTAC 中文报告 | `reports/CPTAC-STAD外部验证报告.md` |
| 评估代码 | `eval_cptac_features.py`、`plot_cptac_feature_results.py`、`audit_first_stage.py` |

当前固定阈值外部验证，来自最新审计表：

- 特征文件：666
- 预测切片：666
- 预测患者：167
- 有标签可评价患者：156
- 阈值策略：固定 `0.5`，不在 CPTAC 上调阈值
- 患者概率聚合：同一患者多张切片概率算术平均

M1 CPTAC 结果：

- AUC：0.8878
- AP：0.8116
- sensitivity：0.8605
- specificity：0.8850
- PPV：0.7400
- NPV：0.9434
- confusion matrix：TN=100、FP=13、FN=6、TP=37

## 2. 第二批材料逐项对照

### 2.1 M1-M6 各 fold 正式权重、最佳 epoch、随机种子、配置、Git commit 及模型登记表

状态：模型登记表和 M1-M4 当前权重可交付；每 fold 正式权重和 best epoch 不可交付。

可交付材料：

| 内容 | 路径 |
|---|---|
| 当前 M1-M4 权重 | `models/M1_immune_sensitive.pt` 等 |
| 模型登记表 | `results/cpu_supplement/model_registry_current.json` |
| 复现 manifest | `results/audit_first_stage/reproducibility_manifest.json` |

限制：

- `models/M1-M4.pt` 是训练脚本保存的每个任务最后一个 fold checkpoint，不是每 fold 完整权重，也不能证明是全数据重训权重。
- M5 pipeline 未保存。
- M6 权重未找到。

### 2.2 各 fold 完整训练日志和超参数搜索记录

状态：不可交付。

原因：

- 没有找到 M1-M6 的完整训练日志、early stopping 记录、全部重复运行记录或超参数搜索记录。
- 当前训练脚本本身也没有完整实验登记/日志保存机制。

可交付替代：

- 训练源码：`src/train_multitask.py`、`src/train_clinical.py`、`src/train_survival.py`
- 现有 metrics JSON：`results/metrics_M*.json`

### 2.3 UNI2-h 特征提取完整配置

状态：CPTAC 外部验证特征提取配置可交付；TCGA 内部原始 UNI2-h 提取配置不完整。

可交付材料：

| 内容 | 路径 |
|---|---|
| CPTAC 特征提取脚本 | `extract_cptac_uni2h_20x256.py` |
| GPU 运行配置 | `results/gpu_deliverables/gpu_deliverables_run_config.json` |
| 特征 manifest | `results/external_cptac_features_20x256/feature_manifest.csv` |
| 特征提取日志 | `results/gpu_deliverables/logs/01_extract_cptac_uni2h_20x256.log` |

当前 CPTAC 配置：

- UNI2-h 权重：`/gpfsdata/home/shitengyuan/shitengyuan_lustre/medical/uni2-h-weights/pytorch_model.bin`
- patch size：20x 下 256
- encoder input：224
- target MPP：0.5
- stride factor：1.0
- tissue threshold：0.35
- mask max size：2048
- max patches：8192
- batch size：64
- seed：42

不能完整交付：

- TCGA 内部特征原始提取日志、QC、颜色处理、归一化细节和特征文件 hash 不完整。

### 2.4 每张切片 QC 清单

状态：CPTAC 有部分清单；正式 WSI QC 不完整。

可交付材料：

| 内容 | 路径 |
|---|---|
| CPTAC 切片状态和排除 | `results/audit_first_stage/cptac_slide_cohort_and_exclusions.csv` |
| CPTAC 特征 manifest | `results/external_cptac_features_20x256/feature_manifest.csv` |
| CPTAC 切片级预测 | `results/external_cptac_feature_infer_20x256_4gpu/external_feature_slide_predictions.csv` |

不能完整交付：

- 每张切片组织面积、扫描参数、伪影人工 QC、切片质量评分。
- TCGA 内部每张切片 QC 清单。

原因：

- 当前脚本记录了文件状态、patch 数和推理状态，但没有人工病理 QC 或扫描仪元数据审计。

### 2.5 15-20 个代表病例证据包

状态：不可完整交付。

可交付替代：

- 错例清单：`results/cpu_supplement/errors_M*_oof.csv`
- 错例汇总：`results/cpu_supplement/error_case_summary.json`
- CPTAC 错例：`results/cpu_supplement/cptac_errors_M*.csv`

不能交付：

- h5
- 坐标
- 缩略图
- attention
- cluster overlay
- 代表 patch

原因：

- TCGA h5 特征和 WSI 视觉衍生物当前不在项目 checkout。
- 没有找到代表病例包生成结果。

### 2.6 Agent 完整工作流、提示词、JSON Schema、工具定义、模型版本、温度和版本历史

状态：部分可交付。

可交付材料：

| 内容 | 路径 |
|---|---|
| Agent 工作流源码 | `src/agent.py`、`run_agent_panel.py` |
| Friday API 客户端 | `src/friday_client.py` |
| 无泄漏 prompt | `results/cpu_supplement/agent_no_leakage_prompt.txt` |
| 无泄漏 schema | `results/cpu_supplement/agent_no_leakage_output_schema.json` |
| 泄漏审计 | `results/audit_first_stage/agent_leakage_audit_and_no_leakage_plan.json` |

不能完整交付：

- 独立版本化的 Agent workflow registry。
- 每次 Agent 运行的完整模型版本历史和稳定性记录。

### 2.7 全部患者 Agent 结构化输出和重复运行稳定性

状态：旧版/开发版部分可交付；正式无泄漏版本不可交付。

可交付：

- `results/agent_judgments.json`
- `results/agent_panel_judgments.json`
- `results/agent_eval.json`

不能作为正式结果：

- 旧输出存在标签/分子亚型泄漏风险。
- panel 只含少量展示病例，不是 289 例全量正式无泄漏输出。
- 无重复运行稳定性实验。

### 2.8 每个无监督 cluster 的代表 patch、聚类中心、分布、命名依据和病理验证

状态：不可交付。

原因：

- 未找到 cluster center、代表 patch、聚类分布表和病理验证记录。
- 当前 Agent 审计已经将视觉模块标记为不可靠/已移除。

### 2.9 M1-M4 全部错误病例清单及亚组结果

状态：错误病例和部分亚组可交付；Lauren、部位、分期、切片质量亚组不可交付。

可交付材料：

| 内容 | 路径 |
|---|---|
| M1 错例 | `results/cpu_supplement/errors_M1_immune_sensitive_oof.csv` |
| M2 错例 | `results/cpu_supplement/errors_M2_msi_oof.csv` |
| M3 错例 | `results/cpu_supplement/errors_M3_ebv_oof.csv` |
| M4 错例 | `results/cpu_supplement/errors_M4_subtype4_oof.csv` |
| 亚组结果 | `results/cpu_supplement/subgroup_metrics_by_site_and_subtype.csv` |
| 亚组汇总 | `results/cpu_supplement/subgroup_metrics_summary.json` |

当前可做亚组：

- TCGA site
- M4 molecular subtype

不能交付：

- Lauren 分型
- 胃部部位
- 分期
- 切片质量

原因：缺少 `clinical.csv` 和切片 QC 表。

### 2.10 校准曲线、Brier score、决策曲线和高敏感度筛查阈值指标

状态：部分可交付。

可交付材料：

| 内容 | 路径 |
|---|---|
| Brier 和校准指标 | `results/audit_first_stage/metrics_M1-M5_oof_recomputed.json` |
| 校准曲线原始数据 | `results/cpu_supplement/calibration_curve_data_oof.csv` |
| 决策曲线原始数据 | `results/cpu_supplement/decision_curve_data_oof.csv` |
| DCA 汇总 | `results/cpu_supplement/calibration_dca_summary.json` |
| CPTAC 阈值扫描图 | `results/external_cptac_feature_infer_20x256_4gpu/figures/fig9_threshold_sweeps_qc_labels.png` |
| CPTAC 调阈值 metrics | `results/external_cptac_feature_infer_20x256_4gpu/figures/cptac_threshold_tuned_metrics_qc_labels.json` |

不能完整交付：

- 内部 OOF 的正式高敏感度筛查阈值表尚未单独整理。
- 决策曲线已有原始数据，但缺少正式论文图版本。

### 2.11 M1 与 M5 配对统计比较，以及图像+临床融合模型真实结果

状态：M1 vs M5 可交付；图像+临床融合不可交付。

可交付：

- `results/cpu_supplement/paired_model_comparison_M1_vs_M5.json`

结果：

- M1 AUC：0.8986
- M5 AUC：0.6201
- AUC 差值均值：0.2790
- bootstrap 95% CI：0.2024 到 0.3555
- bootstrap 双侧 p 值：0.0

不能交付：

- 图像+临床融合模型真实结果。

原因：

- 当前没有看到融合模型训练脚本、OOF、权重或 metrics；M5 是临床基线。

### 2.12 固定权重和固定阈值的外部推理包

状态：可交付。

可交付材料：

| 内容 | 路径 |
|---|---|
| 权重 | `models/M1_immune_sensitive.pt` 到 `models/M4_subtype4.pt` |
| 推理脚本 | `eval_cptac_features.py`、`run_cptac_feature_inference_4gpu.sh` |
| 四卡总控 | `run_gpu_deliverables.sh` |
| 固定阈值结果 | `results/audit_first_stage/cptac_fixed_threshold_metrics_bootstrap.json` |
| 外部推理结果 | `results/external_cptac_feature_infer_20x256_4gpu/` |
| 轻量打包 | `results/gpu_deliverables/package/gpu_deliverables_20260815_140458_refreshed.tar.gz` |

限制：

- 当前权重是 last-fold checkpoint，不是每 fold 正式权重或全数据 refit 权重。

### 2.13 CPTAC 外部验证病例/切片清单、标签来源、特征提取日志、排除记录和患者级结果

状态：可交付。

可交付材料：

| 内容 | 路径 |
|---|---|
| 病例/切片清单和排除 | `results/audit_first_stage/cptac_slide_cohort_and_exclusions.csv` |
| 标签来源 | `results/audit_first_stage/label_sources_and_mapping.json` |
| 原始标签文件 | `/gpfsdata/home/shitengyuan/shitengyuan_lustre/medical/dataset/cptac-stad-histopathology/labels/cptac_stad_2026_supplement/1-s2.0-S2666379126001734-mmc2.xlsx` |
| QC-pass 标签 | `/gpfsdata/home/shitengyuan/shitengyuan_lustre/medical/dataset/cptac-stad-histopathology/labels/cptac_stad_2026_tcga_subtype_labels_qc_pass.csv` |
| 特征日志 | `results/gpu_deliverables/logs/01_extract_cptac_uni2h_20x256.log` |
| 推理日志 | `results/gpu_deliverables/logs/02_infer_cptac_features_4gpu.log` |
| 患者级结果 | `results/audit_first_stage/cptac_patient_cohort_labels_predictions_fixed_threshold.csv` |
| 切片级结果 | `results/audit_first_stage/cptac_slide_predictions_fixed_threshold.csv` |

### 2.14 正式实验版本登记、图表原始数据、绘图脚本、失败实验和不同随机种子结果

状态：版本登记、部分图表原始数据和绘图脚本可交付；失败实验和多随机种子结果不可交付。

可交付材料：

| 内容 | 路径 |
|---|---|
| 复现 manifest | `results/audit_first_stage/reproducibility_manifest.json` |
| GPU run config | `results/gpu_deliverables/gpu_deliverables_run_config.json` |
| CPU supplement manifest | `results/cpu_supplement/cpu_supplement_manifest.json` |
| CPTAC 绘图脚本 | `plot_cptac_feature_results.py` |
| 旧绘图脚本 | `run_plots.py`、`run_plots2.py` |
| 图表原始数据 | `results/cpu_supplement/*.csv`、`results/external_cptac_feature_infer_20x256_4gpu/figures/*.csv` |

不能交付：

- 失败实验系统记录。
- 不同随机种子重复运行结果。
- 完整实验登记表。

原因：

- 现有项目没有统一 experiment tracker，也没有多 seed 训练输出。

## 3. 建议实际交付包

建议先交付以下材料，不包含全部 WSI 和全部 h5 特征：

```text
results/gpu_deliverables/package/gpu_deliverables_20260815_140458_refreshed.tar.gz
```

该包包含：

- 审计脚本和标签重建脚本
- CPTAC 特征提取/推理/绘图脚本
- 第一阶段审计目录
- CPU 补充目录
- CPTAC 推理结果和图表
- CPTAC 特征 manifest 和错误日志
- GPU 运行日志
- 交付说明文档

如果需要目录形式而不是 tar 包，优先交付：

```text
results/audit_first_stage/
results/cpu_supplement/
results/external_cptac_feature_infer_20x256_4gpu/
results/gpu_deliverables/
reports/CPTAC-STAD外部验证报告.md
reports/第一阶段审计交付清单.md
reports/CPU补充材料交付清单.md
reports/GPU交付脚本说明.md
reports/材料需求逐项对照交付说明.md
```

## 4. 当前最关键缺口

若后续要达到论文级复核和外部验证固定版本，最优先补齐：

1. 找回或重建内部 TCGA `clinical.csv`。
2. 找回 TCGA UNI2-h h5 特征和患者-切片映射。
3. 重新训练时保存每 fold 权重、best epoch、train/val 日志、fold 文件和随机种子。
4. 对 M5 保存 fitted pipeline，并明确是否新增图像+临床融合模型。
5. 生成 15-20 个代表病例证据包：thumbnail、attention、cluster overlay、代表 patch、坐标、预测。
6. 用无泄漏 prompt/schema 对 289 例重新运行 Agent，并做重复运行稳定性。
7. 建立 cluster 命名的代表 patch 和病理医师验证表。
8. 对内部 OOF 和 CPTAC 外部验证统一导出固定阈值、高敏感度阈值、校准、DCA 和 bootstrap CI 的论文图表原始数据。

## 5. 2026-08-16 联网补齐更新

按“本地找不到就联网找，缺啥就下载啥”的要求，已进一步补充公开来源材料：

| 材料 | 当前状态 | 路径 |
|---|---|---|
| TCGA-STAD公开临床标签 | 已从 cBioPortal `stad_tcga_pub` API 重建 295例公开临床表；四型构成 EBV 26、MSI 64、GS 58、CIN 147 | `clinical.csv`; `external_downloads/tcga_stad/cbioportal_stad_tcga_pub_clinical_sample_wide.csv`; `external_downloads/tcga_stad/cbioportal_stad_tcga_pub_clinical_patient_wide.csv`; `external_downloads/tcga_stad/cbioportal_public_clinical_build_audit.json` |
| TCGA-STAD patient-slide映射 | 已从 GDC files API 下载1197张 Slide Image 元数据；443例患者；1074张Primary Tumor；442张diagnostic slide；295/295公开标签患者可匹配至少一张Primary Tumor slide，285/295有diagnostic slide | `external_downloads/tcga_stad/gdc_tcga_stad_slide_files.tsv`; `external_downloads/tcga_stad/clinical_with_gdc_slides.csv`; `external_downloads/tcga_stad/gdc_tcga_stad_slide_label_map.csv`; `external_downloads/tcga_stad/gdc_tcga_stad_slide_label_audit.json` |
| 带GDC切片ID的label table | 已生成公开来源版本，可用于核对患者、中心、MSI、EBV、四亚型和GDC切片ID | `results/audit_first_stage/tcga_label_table_from_cbioportal_clinical_with_gdc_slides.csv`; `results/audit_first_stage/tcga_case_flow_from_cbioportal_clinical_with_gdc_slides.json` |
| 三个展示病例原始SVS | 已从 GDC data API 下载 AA69、AA6K、BR-7703 的7张Primary Tumor SVS，合计2,205,315,415 bytes，7/7大小和md5校验通过 | `external_downloads/tcga_stad/showcase_primary_tumor_svs/`; `external_downloads/tcga_stad/gdc_showcase_svs_download_audit.csv`; `external_downloads/tcga_stad/gdc_showcase_svs_download_audit.json` |
| TCGA UNI2-h预提取特征 | 已确认 HuggingFace 目标包 `TCGA/TCGA-STAD.tar.gz` 大小 18,262,777,244 bytes，sha256 `c222adf692dc1be5f9ab14fc51b07c873fbf3f3173f0041a3abf52cec6ed1109`；实际下载失败，因为该dataset为gated，当前HF用户 `sty0515` 未获授权 | `external_downloads/hf_uni2h/tcga_stad_download_status.json` |
| 缺失材料任务台账 | 已更新T01/T07/T10状态，区分“公开来源已补齐”和“仍需历史原始产物/权限/重训”的部分 | `results/cpu_supplement/missing_materials_task_list.csv`; `results/cpu_supplement/missing_materials_task_list.json`; `reports/不可交付材料任务清单与执行进展.md` |

新增结论：

- 公开来源已经足够生成一个可审计的 TCGA-STAD 295例患者-切片-标签清单，但这不是历史训练时的固定版本材料，不能替代旧的 fold 文件、旧权重、旧训练日志和旧特征目录。
- cBioPortal `stad_tcga_pub` 的四型字段不包含 `POLE` subtype；如果要交付“POLE病例清单”，需要先明确是按四分型中的POLE排除，还是按POLE基因突变定义。
- GDC 元数据只补齐 WSI 文件名、file_id、md5、size、sample_type 等索引；除三个展示病例外，没有下载全量大体积原始SVS，也不会自动生成UNI2-h h5、attention heatmap或cluster overlay。
- 三个展示病例的原始SVS已经补齐，但当前环境缺 OpenSlide 共享库，无法直接生成 WSI thumbnail；安装 OpenSlide/libvips 后可基于已下载SVS继续生成缩略图。
- HuggingFace TCGA-STAD UNI2-h特征包当前被权限阻断；解除条件是用已获批的机构邮箱账号登录后重新下载。

## 6. 2026-08-16 缺失清单继续完成进展

随后使用获授权 HuggingFace token 完成了 TCGA-STAD UNI2-h 特征包下载，并继续补齐以下材料：

| 材料 | 当前状态 | 路径 |
|---|---|---|
| TCGA-STAD UNI2-h h5特征 | 已下载、sha256校验通过并解压；400个h5，375例患者，全部可读、全部含coords，feature维度均为1536 | `external_downloads/hf_uni2h/TCGA/TCGA-STAD.tar.gz`; `tcga_stad_uni2h/TCGA-STAD/features/`; `results/audit_first_stage/tcga_uni2h_feature_manifest.csv`; `results/audit_first_stage/tcga_uni2h_feature_manifest_summary.json` |
| 公开feature-matched队列 | 公开295例标签中246例可匹配到h5；49例缺对应h5。246例中CIN 128、GS 50、MSI 45、EBV 23 | `results/audit_first_stage/tcga_public_feature_matched_246_cohort.csv`; `results/audit_first_stage/tcga_public_label_patients_missing_uni2h_features.csv`; `results/audit_first_stage/tcga_public_feature_matched_audit_summary.json` |
| 公开feature-matched fold | 已生成5折patient-level/site-group fold和summary；这是公开246例重建fold，不是历史训练原始fold | `results/audit_first_stage/tcga_public_feature_matched_cv_folds.csv`; `results/audit_first_stage/tcga_public_feature_matched_cv_fold_summary.csv` |
| 三个展示病例证据包 | 已补h5路径、coords key、patch数、GDC原始SVS路径和md5校验；缩略图/attention/cluster图仍未生成 | `results/cpu_supplement/showcase_cases_evidence_with_h5_svs.csv`; `results/cpu_supplement/showcase_cases_evidence_with_h5_svs.json` |
| 20例代表病例候选包 | 已覆盖TP/TN/FP/FN和边界病例，20例均有h5/coords；尚不是可视化完整证据包 | `results/cpu_supplement/representative_case_candidate_package_20.csv`; `results/cpu_supplement/representative_case_candidate_package_20_summary.json` |
| 无监督cluster数值证据 | 已生成8个cluster中心、代表patch坐标、样本分布和患者cluster fraction；未做病理命名 | `results/cpu_supplement/unsupervised_cluster_numeric_evidence/` |
| M5临床缺失率和预处理 | 已生成公开临床字段缺失率、M5特征缺失率和预处理摘要 | `results/audit_first_stage/tcga_public_raw_clinical_missingness.csv`; `results/audit_first_stage/tcga_public_M5_clinical_feature_missingness.csv`; `results/audit_first_stage/tcga_public_M5_clinical_preprocessing_summary.json` |
| GPU重训入口 | 已新增统一脚本，当前节点无GPU未执行正式重训 | `run_internal_public_retrain.sh` |

仍不能交付的核心项：

- 历史训练固定版本的原始fold、每fold权重、best epoch和日志：旧产物不存在，不能由OOF反推。
- 新正式M1-M4/M6重训结果：当前节点 `nvidia-smi` 不可见GPU；数据和脚本已经准备好。
- WSI thumbnail、attention heatmap、cluster overlay和代表patch图片：当前环境缺OpenSlide共享库，且attention导出需要模型权重/重训流程。
- cluster病理命名和医师验证：已生成数值cluster，但没有代表patch图像和病理医师确认，不能命名为TIL/肿瘤/间质/坏死。
- 无泄漏Agent全量重跑：当前环境没有可用 `FRIDAY_APP_ID`/LLM app配置。
